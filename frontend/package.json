{
  "name": "frontend",
  "version": "1.0.0",
  "private": true,
  "description": "SimpleHealth frontend (vanilla HTML/CSS/JS) served as a standalone static app",
  "scripts": {
    "start": "http-server . -p 3000 -c-1"
  },
  "dependencies": {
    "http-server": "^14.1.1"
  }
}
