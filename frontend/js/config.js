// The frontend is now a standalone static app, served separately from the
// backend API — so this needs an absolute URL instead of a relative path.
// Change this if you deploy the backend somewhere other than localhost:5000.
const API_BASE = "http://localhost:5000/api";
